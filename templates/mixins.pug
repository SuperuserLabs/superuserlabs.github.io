mixin section(padding="2em 0em")
    div()&attributes(attributes)
      div(style=`padding: ${padding}`)
        .mdl-grid
          +cell(8).mdl-cell--2-offset-desktop
            .mdl-grid
              block


mixin button(href)
  a.mdl-button.mdl-js-button.mdl-button--raised(href=href, style='margin: 0 2em 1em 0;')&attributes(attributes)
    block


mixin navlink(href)
  a.mdl-navigation__link(href=href)
    block


mixin nav
  nav.mdl-navigation
    block


mixin text(color='black')
  span(class=`mdl-color-text--${color}`)
    block


mixin spacer
  .mdl-layout-spacer


mixin cover(background, style)
  div(style=`position: relative; background: ${background}; background-repeat: no-repeat; background-size: cover; ${style}`)
    block


mixin text(tag, typography, color='black')
  #{tag}(class=`${typography.split(' ').map(x => 'mdl-typography--' + x).join(' ')} mdl-color-text--${color}`)
    block


mixin typo(tag, ...classes)
  #{tag}(class=classes.map(x => `mdl-typography--${x}`).join(' '))
    block


mixin layout
  .mdl-layout.mdl-js-layout.mdl-layout--fixed-header
    block


mixin mlayout(tag, cls)
  #{tag}(class=`mdl-layout__${cls}`)
    block


mixin fa(name, group='fas', color='black')
  i(class=`${group} fa-${name} fa-8x mdl-color-text--${color}`)


mixin cell(cols)
  div(class=`mdl-cell mdl-cell--${cols}-col`)&attributes(attributes)
    block


mixin sectitle
  h2.mdl-typography--display-2-color-contrast
    block

mixin footer
  +section("2em")(class!=attributes.class)
    +cell(12)
      p(style="margin: 0")
        b Made with #[i.fas.fa-heart(style="color: red")] by Superuser Labs
